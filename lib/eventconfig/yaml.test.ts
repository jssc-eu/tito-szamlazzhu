import { YamlConfig } from './yaml'

describe('parse kdl config', () => {

  let config

  beforeAll(() => {
    config = new YamlConfig('integration-test-event-2022', './test-config.yaml')
  })

  test('', () => {});
})
