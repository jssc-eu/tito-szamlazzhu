import { KdlConfig } from './kdl'

describe('parse kdl config', () => {

  let config

  beforeAll(() => {
    config = new KdlConfig('integration-test-event-2022', './test-config.kdl')
  })

  test('', () => {});
})
